# Local Claude Code state (not shared) — settings.local.json can hold
# machine-specific permission overrides; skills/ and other shared config stay tracked.
.claude/settings.local.json

# Local MCP connection config may contain bearer tokens
.codex/config.toml
.mcp.json

# Node / Electron
node_modules/
dist/
out/
# @electron/packager output — HyppoVisor-<platform>-<arch>/
HyppoVisor-*/
# electron-builder output (feature 010) + the inventory it regenerates each run
release/
THIRD-PARTY-LICENSES
*.log
.vitest/
test-results/
playwright-report/
.env
.env.*

# OS
.DS_Store

# design-phase reference material — kept locally, not published
specs/initial/

# field-captured issue notes / activity logs — kept locally, not published
specs/issues/
